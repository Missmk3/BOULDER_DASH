package entity;

/**
 * enum Permeability
 * @author GROUP 09
 */
public enum Permeability {
    /** The blocking. */
    WALL,
    /** The penetrable. */
    WALKABLE,
    /** The killing. */
    KILLABLE,
    /** The pushing. */
    BOULDER,
    /** The win. */
    EXIT,
    /** The diggable. */
    DIGGABLE,
    /** The diamond. */
    DIAMOND;

}
